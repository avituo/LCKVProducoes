import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ratio.lckv',
  appName: 'LCKVApp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
